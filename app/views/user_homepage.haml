!!!



.category
  %h1{:id => "heading", :style => "color: #{current_user.color}"} CREATE SPLASH
  %form{:action => "/users/splash/new", :method => "post"}
    %input{:id => "new-splash", :type => "text", :name => "content", :placeholder => "#{current_user.color.upcase} says...", :style => "border: 5px solid #{current_user.color}"}
    %input{:id => "splash-button", :type => "submit", :value => "SPLASH", :style => "background-color: #{current_user.color}"}

.category
  %h1{:id => "heading", :style => "color: #{current_user.color}"} #{current_user.color.upcase}'s SPLASH FEED

- @splashes.each do |splash|
  .splash
    .splash-text{:style => "background-color: #{splash.user.color}" }
      %a=splash.content




/{:style => "background-color: #{User.(splash.user_id).color}"}
/ -if current_user.splashes.any?
/   %ul
/     -@splash_contents.each do |content|
/       %li
/         =content
/ -else
/   %h1 your splashes here!
