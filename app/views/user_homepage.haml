!!!


/ Make a button for homepage
/ We should use the @user.color to also color the background of the page
%h1= current_user.color

%h2 add a splash of #{current_user.color} !
%form{:id => "new-splash", :action => "/users/splash/new", :method => "post"}
  %input{:type => "text", :name => "content", :placeholder => "splash"}
  %input{:type => "submit", :value => "splash!"}

%h3 #{current_user.id}

-if current_user.splashes.any?
  %ul
    -@splash_contents.each do |content|
      %li
        =content
-else
  %h1 your splashes here!


/ .splash{:style => "background-color:#{users.color}"}
/   .splash-text
/     %p=splash

/ color of the user
